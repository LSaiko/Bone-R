# YOLO dataset config for the Phase-1 harmonized split (CLAHE + repaired +
# label-safe resized images). Mirrors dataset.yaml but points at dataset_v3.
path: dataset_v3
train: images/train
val: images/val
test: images/test

names:
  0: fracture
